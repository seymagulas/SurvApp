import { render, screen } from '@testing-library/react';
import MultiChoiceQuestion from '../../components/questions/MultiChoiceQuestion';
import '@testing-library/jest-dom'
import '@testing-library/jest-dom/extend-expect'


describe('MultiChoiceQuestion component', () => {
  it('should add a new input every time addAnswer is clicked', () =>{

  });

  it('should add a newTopic to answer array if AiOutlinePlusCircle is clicked', () =>{

  });

  it('should delete the newTopic to from the answer array if BsTrash is clicked', () =>{

  });
  it('the page should be render every time there is a new Topic to be showed', () =>{

  });
  it('the page should be render every time addAnswer is clicked', () =>{

  });
  it('the page should be render every time BsTrash is clicked', () =>{

  });
})